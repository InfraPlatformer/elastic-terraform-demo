apiVersion: v1
kind: Service
metadata:
  name: kibana-loadbalancer
  namespace: kibana
  labels:
    app: kibana
spec:
  selector:
    app: kibana
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5601
  type: LoadBalancer

